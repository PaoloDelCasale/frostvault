"""Focused, single-purpose application services.

These framework-agnostic modules own business logic that is too large or too
sensitive to inline directly in ``app.main`` route handlers, including the
owner/operator/viewer matrix and ownership-transfer rules.
"""
