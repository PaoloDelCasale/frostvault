# Behavioral Contract Extraction

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-07-23 · Project: FrostVault

Generated: 2026-07-23
Source files analyzed: 83 code (scoped to Phase 1 high-risk subsystems + supporting auth/crypto/jobs)
Total contracts extracted: 72
Scope note: Full-breadth first pass over auth, storage jobs, catalog identity, cloud deletion, metadata backups, DB schema gate, vault roles — not exhaustive every migration SQL file or Traefik/infra.

## Summary by category

- METHOD: 28
- NULL: 6
- CONFIG: 8
- ERROR: 9
- INVARIANT: 12
- COMPAT: 2
- ORDER: 1
- LIFECYCLE: 4
- THREAD: 2

---

### app/services/vault_roles.py (6 contracts)

1. [METHOD] `can_operate(role)`: returns True only for `owner` and `operator`
2. [METHOD] `is_owner(role)`: returns True only for `owner`
3. [INVARIANT] Freeing local space is documented as an owner-only destructive lifecycle action
4. [INVARIANT] Operators may upload/recover but never touch membership/policy
5. [INVARIANT] Viewers are strictly read-only
6. [COMPAT] Matrix must stay aligned with HTTP enforcement and tests (README Users section currently conflicts)

### app/storage.py — recovery download (8 contracts)

7. [METHOD] `download_exact_version_plaintext`: raises if `provider_version_id` is empty
8. [INVARIANT] Path-only current-object downloads are forbidden
9. [METHOD] Crypt name-encrypt path: rclone `copyto` with `--s3-version-id=` against logical `job['path']`
10. [METHOD] Content-encrypt (names plaintext) path: rclone against configured destination + version flag
11. [METHOD] Plain path: boto3 `get_object` with `Bucket`, `Key=object_key`, `VersionId`
12. [ERROR] Missing temporary file after rclone raises RuntimeError
13. [INVARIANT] Both surfaces must pin the same Archive Version identity; digest mismatch fails the Job
14. [LIFECYCLE] `ensure_job_active` checked before/during download

### app/storage.py — free local space (7 contracts)

15. [METHOD] `process_free_space`: claim via rename, rehash, unlink claim
16. [METHOD] On hash/VersionId failure: `restore_claimed_local_copy` restores Local Copy
17. [ERROR] If claim disappears mid-restore, raise hard error citing claim path
18. [INVARIANT] Must not delete Local Copy unless fingerprint matches verified available Archive Version
19. [NULL] Missing target / version aborts safely without unlinking original
20. [LIFECYCLE] Claim restore is the fallback when verification fails after claim
21. [INVARIANT] API 202 queue success ≠ Local Copy already gone (worker enforces safety)

### app/storage.py — cloud archive / rename / purge (9 contracts)

22. [METHOD] `process_cloud_archive`: `delete_object(Bucket, Key)` without VersionId (delete marker on current)
23. [ERROR] Missing Delete Marker VersionId from S3 raises RuntimeError
24. [METHOD] Records marker via `record_delete_marker` and `record_archive_completed`
25. [METHOD] `process_rename` hide-old-key: unversioned `delete_object` after verify
26. [METHOD] `process_cloud_purge`: versioned delete of noncurrent versions / markers
27. [INVARIANT] Reversible hide must not silently hide an unexpected newer version without detection/audit
28. [INVARIANT] Purge pins VersionId; archive/rename currently do not
29. [CONFIG] Cloud deletion disabled vault refuses archive (`cloud_deletion_enabled`)
30. [ERROR] Missing archive_version target raises RuntimeError

### app/storage.py — job dispatcher (8 contracts)

31. [METHOD] `process_job`: cancelled status → return False, no work
32. [METHOD] upload only for status in {queued, retrying}
33. [METHOD] rename only for status == queued
34. [METHOD] recover currently runs for any non-cancelled status reaching the branch (gap vs peers)
35. [METHOD] free-space / cloud-archive only for queued
36. [METHOD] cloud-purge for {queued, pending_delay}
37. [METHOD] restoring + age < poll interval → return False (defer)
38. [INVARIANT] Unknown action/status combination returns False without failing the job

### app/sessions.py (6 contracts)

39. [METHOD] `resolve_session`: rejects revoked, expired absolute/idle, inactive user
40. [METHOD] `resolve_session`: rejects when `session_version` ≠ user's current version
41. [METHOD] `resolve_session`: refreshes `last_seen_at` / idle expiry on success
42. [METHOD] `csrf_token_for`: read-only; never touches last_seen_at
43. [METHOD] `csrf_token_for`: rejects revoked/expired sessions
44. [INVARIANT] Should-exist: CSRF lookup ought to honor session_version parity with resolve_session (currently does not)

### app/proxy.py + app/breakglass.py (8 contracts)

45. [METHOD] `resolve_client_ip`: without trusted peer, returns socket peer (ignore XFF)
46. [METHOD] `resolve_client_ip`: with trusted peer, walks XFF from the right for first untrusted hop
47. [METHOD] If all XFF hops trusted, returns `hops[0]`
48. [NULL] Missing peer → None
49. [METHOD] `is_break_glass_allowed`: fail-closed on None/unparseable IP
50. [INVARIANT] Loopback always allowed regardless of CIDR config
51. [CONFIG] Allowed = loopback ∪ BREAK_GLASS_ALLOWED_CIDRS
52. [ERROR] Bad CIDR parse in settings → deny (fail-closed)

### app/services/metadata_backups.py (5 contracts)

53. [METHOD] `verify_restore_isolated` PG: `pg_restore --list` must succeed or raise
54. [METHOD] Primary path: createdb + pg_restore + count query
55. [METHOD] If createdb fails after list ok: returns `ok: True`, `verification_mode: "pg_restore_list"`
56. [INVARIANT] Should-exist: list-only mode must not be treated as full verified restore by consuming UIs
57. [ERROR] Hard pg_restore failure (rc > 1) raises BackupError

### app/database.py (4 contracts)

58. [INVARIANT] `HEAD_SCHEMA_REVISION` must equal alembic_version or raise `DatabaseSchemaError`
59. [METHOD] `SQLiteConnection.execute` translates `%s` → `?`
60. [LIFECYCLE] Context manager commits on success, rolls back on exception
61. [CONFIG] Backend selected by `settings.db_backend`

### app/services/operation_policies.py (4 contracts)

62. [METHOD] `_normalize_windows`: start must be strictly before end (no overnight spans)
63. [ERROR] Invalid weekday/time raises ValueError
64. [CONFIG] auto_local_cleanup requires local_retention_days
65. [METHOD] `policy_allows_transfer_now` delegates to operating window check

### app/services/vault_recovery.py (3 contracts)

66. [METHOD] `require_upload_custody`: raises `RecoveryCustodyRequired` when crypt vault needs custody confirmation
67. [INVARIANT] Upload blocked until recovery-secret custody confirmed for crypt vaults
68. [ERROR] Message must not leak secret material

### app/catalog.py — rename identity (3 contracts)

69. [METHOD] `list_rename_candidates`: `auto` only for unambiguous 1:1 digest matches
70. [METHOD] Ambiguous matches require human confirmation via `confirm_file_rename`
71. [INVARIANT] Stable Vault File identity per ADR 0001

### app/main.py — HTTP authorization surface (3 contracts)

72. [METHOD] `/api/free-space` enforces `is_owner` (+ reauth + ALLOW_LOCAL_DELETE)
73. [METHOD] Operators allowed upload/recover; viewers denied mutations
74. [COMPAT] README § Users claims operators can free local space — conflicts with code/tests and README § Local cleanup safety

## Parity iteration contracts (REQ-028..030)

75. [INVARIANT] Metadata backup verify stamps only the run whose artifact was restore-tested (REQ-028 / BUG-016)
76. [INVARIANT] After Delete Marker success, cloud-archive notify failures are best-effort and must not retry hide (REQ-029 / BUG-017)
77. [INVARIANT] Cancel recover retains archive_versions.restore_state after RestoreObject accept (REQ-030 / BUG-018)

