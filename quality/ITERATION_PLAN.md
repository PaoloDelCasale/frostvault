# Iteration Plan — parity strategy

> Generated by Quality Playbook v1.5.10 — FrostVault parity iteration
> Date: 2026-07-24 · Prior: baseline Phases 1–6 + gap + unfiltered (gate_verdict=pass)

## Strategy

**parity** — Systematically enumerate parallel implementations of the same
contract and diff them for inconsistencies (equivalent / inverse / symmetric
behavior). Do **not** invent symmetry where product requirements intentionally
differ.

## Mode

Mode B canonical `iteration_prompt(parity)` executed **in-process**
(`B-prompt-in-process`): nested Copilot/Cursor CLI is not available in this
environment (`Copilot CLI not installed` / `'cursor' CLI not found`). Nested
`python3 -m bin.run_playbook --next-iteration --strategy parity` aborted during
orchestration; same fallback as completed gap and unfiltered iterations.
Evidence of aborted nested attempt: `quality/logs/20260724T083348Z/`.

## Lightweight prior-run scan (dedup only)

Active confirmed bugs (must preserve): BUG-001, BUG-002, BUG-004, BUG-005,
BUG-006, BUG-007, BUG-008, BUG-009, BUG-010, BUG-011, BUG-012, BUG-013,
BUG-014, BUG-015.

Rejected / demoted (must preserve): former BUG-003; CAND-006; DC-001..DC-012.

Next new IDs start at **BUG-016** only for genuinely net-new confirmed defects.

## Parity inventory (built from repository before pairwise review)

| # | Pair / family | Left path | Right path |
|---|---------------|-----------|------------|
| P01 | Upload vs recover | `process_upload` | `process_recover` |
| P02 | Archive vs recover | `process_cloud_archive` | `process_recover` |
| P03 | Local free-space vs cloud archive | `process_free_space` + `record_automatic_cleanup_outcome` | `process_cloud_archive` + `record_archive_completed` |
| P04 | Create vs delete (cloud) | upload VersionId record | delete_object / Delete Marker |
| P05 | Rename vs delete/recover | `process_rename` | archive/recover object_key |
| P06 | Metadata backup vs verify | `run_metadata_backup` | `_verify_latest_metadata_backup_once` / download |
| P07 | Success vs restart recovery | job complete paths | startup / cancel / reclaim |
| P08 | Normal vs retry | upload retry policy | cloud-archive failure → requeue |
| P09 | Initial scan vs rescan | local/cloud scan | watcher / rescan |
| P10 | Scan before vs after rename | rename Path History | cloud scan identity |
| P11 | Activation vs deactivation | `update_user` active | password / session_version |
| P12 | OIDC login vs invite / self-link | callback bind | admin invite + `/auth/oidc/link` |
| P13 | Owner vs operator | vault_roles matrix | API Depends |
| P14 | UI vs backend authz | templates / JS | FastAPI Depends |
| P15 | Session create vs logout / invalidate | `create_session` | revoke / version bump |
| P16 | Versioned upload vs hide | upload VersionId | cloud-archive Delete Marker |
| P17 | Plaintext vs crypt download | boto3 GetObject | rclone crypt |
| P18 | Object key generate vs consume | upload/rename keys | recover/archive keys |
| P19 | S3 backup configured vs unavailable | `default_object_store` | local-only success |
| P20 | Job create vs recover dispatcher | peer action whitelists | recover branch |
| P21 | User cancel vs AWS RestoreObject | cancel recover | `restore_state` persistence |
| P22 | Success vs exception cleanup | free-space observability | cloud-archive observability |
| P23 | Price estimate vs worker billing | `/api/recover/estimate` | `process_recover` |
| P24 | Config-time vs execution-time rates | `CostPriceBookCreate` | `estimate_restore` lookup |
| P25 | Audit create/rename/delete | various `audit_log` / `record_audit_event` | connection-bound vs unbound |
| P26 | Notification declare vs deliver | enqueue events | delivery clients |

## Out of scope (preserve)

- Do not restart baseline or overwrite prior EXPLORATION.md / ITER2 / ITER3.
- No production source edits; fix patches only under `quality/patches/`.
- Do not reopen former BUG-003 or DC-001..012 without formal re-evaluation
  evidence stronger than prior dismissal.

## Success criteria (iteration completion gate)

- [x] This `ITERATION_PLAN.md` names strategy `parity`
- [x] `EXPLORATION_ITER4.md` ≥ 80 substantive lines
- [x] `EXPLORATION_MERGED.md` updated with `[Iteration 4: parity]` attribution
- [x] ≥2 net-new candidates not in previous iterations
- [x] ≥1 finding from cross-path comparison not driven by prior strategies alone
- [x] Full Phases 2–6 workflow for net-new bugs + RED→GREEN
- [x] Regression baseline starts at 14 expected failures until net-new tests added
