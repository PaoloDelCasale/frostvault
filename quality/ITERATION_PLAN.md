# Iteration Plan — unfiltered strategy

> Generated by Quality Playbook v1.5.10 — FrostVault unfiltered iteration
> Date: 2026-07-24 · Prior: baseline Phases 1–6 + gap iteration (gate_verdict=pass)

## Strategy

**unfiltered** — Pure domain-driven exploration without structural scaffolding.
Do **not** archive or replace `quality/`. Build on baseline + gap history.
Do **not** constrain exploration to the prior gap list, pattern matrices, or
prior EXPLORATION section structure. Prior artifacts are for deduplication only.

## Mode

Mode B canonical `iteration_prompt(unfiltered)` executed **in-process**
(`B-prompt-in-process`): nested Cursor CLI is not available in this environment
(`ERROR: 'cursor' CLI not found`). Same fallback as the completed gap iteration.

## Lightweight prior-run scan (dedup only — not an exploration map)

Active confirmed bugs (must preserve): BUG-001, BUG-002, BUG-004, BUG-005,
BUG-006, BUG-007, BUG-008, BUG-009, BUG-010, BUG-011.

Rejected / demoted (must preserve): former BUG-003; CAND-006; DC-001, DC-002, DC-003.

Next new IDs start at **BUG-012** only for genuinely net-new confirmed defects.

## Unfiltered exploration intent

Explore as a domain expert in cold-storage vaults would: follow recover/upload/
free-space/restart paths, identity & session transitions, backup/DR fitness,
local↔cloud discrepancies, silent success, concurrency, and authorization edges —
without required section templates or applicability matrices.

## Out of scope (preserve)

- Do not restart baseline or overwrite prior EXPLORATION.md / gap ITER2.
- No production source edits; fix patches only under `quality/workspace/patches/`.
- Do not reopen former BUG-003 list-only metadata verify without stronger
  independent evidence and playbook challenge protocol.

## Success criteria (iteration completion gate)

- [x] This `ITERATION_PLAN.md` names strategy `unfiltered`
- [x] `EXPLORATION_ITER3.md` ≥ 80 substantive lines
- [x] `EXPLORATION_MERGED.md` updated with `[Iteration 3: unfiltered]` attribution
- [x] ≥2 net-new candidates not in previous iterations
- [x] ≥1 finding in a code area / failure mode not structurally driven by prior runs
- [x] Full Phases 2–6 workflow for net-new bugs + RED→GREEN
- [x] Regression baseline remains 10 expected failures until net-new tests added
