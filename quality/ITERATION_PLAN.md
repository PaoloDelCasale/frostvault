# Iteration Plan — adversarial strategy

> Generated by Quality Playbook v1.5.10 — FrostVault adversarial iteration
> Date: 2026-07-24 · Prior: baseline Phases 1–6 + gap + unfiltered + parity (gate_verdict=pass)

## Strategy

**adversarial** — Re-investigate dismissed, demoted, and thinly SATISFIED
verdicts to recover Type II errors. Not a full re-exploration; evidence must
invalidate prior dismissal reasoning.

## Execution mode

Mode B canonical `iteration_prompt(adversarial)` executed **in-process**
(`B-prompt-in-process`): nested Copilot/Cursor CLI is not available in this
environment (`! Copilot CLI not installed`). Canonical attempt
`python3 -m bin.run_playbook --next-iteration --strategy adversarial` aborted
during orchestration; same fallback as completed gap, unfiltered, and parity
iterations.

Evidence of aborted nested attempt: `quality/logs/20260724T091311Z/`.

## Pass inventory

### Pass A — Attack rejected / demoted findings

| ID | Prior verdict | Re-promotion criteria (summary) | Adversarial plan |
|----|---------------|----------------------------------|------------------|
| former BUG-003 | REJECTED | Authoritative contract forbidding list-only verified | Re-read challenge gate; probe list-only path only |
| CAND-006 | REJECTED | rclone crypt drops VersionId pin | Code + digest backstop; no MinIO crypt failure |
| DC-001 | DEMOTED | ADR requiring auth_time/max_age | ADR-0005 quote check |
| DC-002 | DEMOTED | Concurrent PG double-redeem | Structural TOCTOU vs ADR-0003 |
| DC-003 | DEMOTED | Operator cancel succeeds / false success | 403-only UX |
| DC-004 | DEMOTED | User-authored file deleted by reconcile | Intentional + tested |
| DC-005 | DEMOTED | Tier1/2 reauth for backup download | ADR scope check |
| DC-006 | DEMOTED | Concurrent deactivate → 0 admins | Separate txn race vs stated invariant |
| DC-007 | DEMOTED | CSRF GET binds attacker subject | E2E CSRF+OIDC chain |
| DC-008 | DEMOTED | Prefs must deliver required event | Wiring / no integrity |
| DC-009 | DEMOTED | Spec promises resume without SQL | No API promise |
| DC-010 | DEMOTED | Must preserve foreign tags | No merge requirement |
| DC-011 | DEMOTED | temp_database ok:True when restore failed | Distinct from list-only |
| DC-012 | DEMOTED | actor_user_id required on owner audits | Hygiene only |
| DC-013 | DEMOTED | ADR requiring reauth on self-link | ADR-0005 invite creation |
| DC-014 | DEMOTED | Config normalization contract + silent €0 | Contract bar |
| DC-015 | DEMOTED | Spec requires archive_failed audit | Observability vs BUG-017 |
| SA-M HeadObject | FALSE-POSITIVE vs REQ-001 | REQ-001 recovery-only scope | Reconfirm scope |
| intentional asymmetries | n/a | Owner/operator, purge vs archive | Pass C + thin SATISFIED |

### Pass B — Challenge thin SATISFIED

Targets: REQ-001 recovery VersionId pins; REQ-005 list-only vs temp_database;
consumer `status=verified` on `ok` alone; estimate/worker price-book (BUG-015);
authorization at UI vs dispatcher; audit without connection (BUG-009);
metadata verify without identity bind (BUG-016).

### Pass C — Attack confirmed-bug boundaries

All active BUG-001..018: duplicate check, scope width, RED→GREEN fidelity,
sibling paths (esp. BUG-002 rename hide, BUG-015 second estimate, BUG-017
notify-only fix vs claim).

## Iteration completion gate checklist

- [x] This `ITERATION_PLAN.md` names strategy `adversarial`
- [x] `EXPLORATION_ITER5.md` ≥ 80 lines substantive content
- [x] `EXPLORATION_MERGED.md` updated with `[Iteration 5: adversarial]` attribution
- [x] ≥ 2 new candidates not present previously (or re-confirmed dismissals with fresh evidence)
- [x] At least 1 finding re-confirms a previously dismissed finding with fresh evidence OR covers new adjacent code
