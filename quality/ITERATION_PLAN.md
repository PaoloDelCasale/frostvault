# Iteration Plan — gap strategy

> Generated by Quality Playbook v1.5.10 — FrostVault gap iteration
> Date: 2026-07-24 · Prior run: baseline Phases 1–6 (gate_verdict=pass)

## Strategy

**gap** — Scan previous coverage, explore uncovered / thinly explored areas.
Do **not** archive or replace `quality/`. Build on the completed baseline.

## Prior-run coverage map (lightweight)

| Section | Subsystems | Depth | Notes |
|---------|------------|-------|-------|
| Open Exploration Findings | auth sessions, storage jobs, free-space, cloud hide | deep | Fed BUG-001..007 |
| Quality Risks | jobs, VersionId, CSRF, README, metadata verify | moderate | Risks traced selectively |
| Pattern: Fallback | Archive Version download, free-space claim, metadata verify | shallow–moderate | Cascades short |
| Pattern: Dispatcher | `process_job` | moderate | Recover whitelist → BUG-001 |
| Pattern: Cross-impl | free-space authz, S3 hide, DB adapters | shallow | DB adapters barely touched |
| Pattern: Composition | break-glass+proxy, session vault membership | shallow–moderate | Proxy/OIDC edges thin |
| Candidate Bugs | CAND→BUG path | deep | 6 active + rejected former-003 |

## Baseline scope (from PROGRESS.md)

**Covered:** AuthN/AuthZ & sessions; vault crypto/recovery/governance; catalog free-space/recover/upload; job scheduler & windows; cloud deletion/lifecycle; schema gate; HTTP role checks.

**Deferred (explicit gaps):** deep Traefik/infra Terraform; exhaustive migration SQL; exhaustive frontend interaction matrix.

## Gap targets for this iteration

1. **OIDC / invites / step-up reauth** — `app/oidc.py`, `app/invites.py`, `/auth/oidc/*` in `main.py` (thin in baseline Composition section).
2. **Catalog identity after rename + cloud scan** — `ArchiveCatalog._get_or_create_file` / `record_archive_version` / `scan_cloud` (Path History invariance underspecified in exploration).
3. **Metadata backup off-host path** — `default_object_store` + `backup_upgrade` / `run_metadata_backup` when bucket configured but client fails.
4. **Durable audit for Path History mutations** — `audit_log` without `connection` on rename confirm paths.
5. **Frontend vs server cancel authz** — operator Stop button vs owner-only cloud-archive cancel (UX mismatch; demoted unless authz bypass).

## Out of scope (preserve)

- Do not restart baseline; do not overwrite prior EXPLORATION.md.
- Preserve active BUG-001,002,004,005,006,007 and rejected former BUG-003 / CAND-006.
- Assign BUG-008+ only to net-new confirmed defects.
- No production source edits; fix patches only under `quality/workspace/patches/`.

## Success criteria (iteration completion gate)

- [x] This `ITERATION_PLAN.md` names strategy `gap`
- [x] `EXPLORATION_ITER2.md` ≥ 80 substantive lines
- [x] `EXPLORATION_MERGED.md` with attribution + Demoted Candidates
- [x] ≥2 net-new candidates not in baseline
- [x] ≥1 finding in previously unexplored area
- [x] Full Phases 2–6 workflow for net-new bugs + RED→GREEN
