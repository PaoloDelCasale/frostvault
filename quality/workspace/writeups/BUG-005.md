# BUG-005: README Users overclaims operator free-space

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.6 — Andrew Stellman
> Date: 2026-07-23 · Project: FrostVault

## 1. Summary

README Users says operators can free local space; code and Local cleanup safety restrict that to owners.

## 2. Spec reference

- Requirement: REQ-003
- Behavioral contract quote: "owners only free local space"
- REQ-003 Pattern compensation: docs must not advertise operator free-space. Authoritative: `vault_roles.is_owner`, `main.py:2451`, README Local cleanup safety.

## 3. The code

Runtime denies operators (`is_owner` False). Defect is `README.md:19-20` wording.

## 4. Observable consequence

Operators following docs hit 403; support pressure may incorrectly widen authorization.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-005-fix.patch`:

```diff
diff --git a/README.md b/README.md
index 74df81e..af1a817 100644
--- a/README.md
+++ b/README.md
@@ -16,8 +16,8 @@ Search remains global across every file in the selected vault.
 - Each vault has its own local folder, S3 prefix, and Rclone remote.
 - Every query and operation includes the vault identifier.
 - Users can see only vaults assigned to them.
-- The primary `owner` manages sharing and policy; `operator` can upload, free
-  local space, and recover files; `viewer` is read-only.
+- The primary `owner` manages sharing and policy and may free local space;
+  `operator` can upload and recover files; `viewer` is read-only.
 - Queued or active uploads, recoveries, and local cleanup operations can be
   stopped with the **Stop** button shown next to their progress.
 - Operations run in parallel. `OPERATION_CONCURRENCY` controls the number of
```

## 7. The test

- Regression test: `quality.test_regression.TestBug005.test_bug_005_readme_users_owner_only_free_space`
- Regression patch: `quality/workspace/patches/BUG-005-regression-test.patch`
- Fix patch: `quality/workspace/patches/BUG-005-fix.patch`
- Red receipt: `quality/workspace/results/BUG-005.red.log`
- Green receipt: `quality/workspace/results/BUG-005.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (Phase 5 verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
