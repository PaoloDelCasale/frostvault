# BUG-009: Rename confirmations omit audit_log connection

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Gap iteration

## 1. Summary

Path History rename mutations emit log-only audit lines and never insert audit_events rows.

## 2. Spec reference

- Requirement: REQ-021
- Behavioral contract quote: "audit_log persists when connection is provided so /api/audit-events shows govern"
- CoS: Gap iteration EXPLORATION_ITER2.md / EXPLORATION_MERGED.md

## 3. The code

- Location: `app/main.py:1494-1501; app/storage.py:801-808`
- Documented intent: audit_log persists when connection is provided so /api/audit-events shows governance mutations.
- Code behavior: confirm_rename / confirm_folder_rename / apply_auto_renames call audit_log without connection inside an open db() transaction.

## 4. Observable consequence

Path History rename mutations emit log-only audit lines and never insert audit_events rows.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-009-fix.patch`:

```diff
--- a/app/main.py
+++ b/app/main.py
@@ -1493,6 +1493,7 @@
         )
         audit_log(
             "vault_file_renamed",
+            connection,
             vault_id=vault["id"],
             vault_file_id=action.vault_file_id,
             new_path=new_path,
@@ -1537,6 +1538,7 @@
         )
         audit_log(
             "vault_folder_renamed",
+            connection,
             vault_id=vault["id"],
             old_prefix=old_prefix,
             new_prefix=new_prefix,
--- a/app/storage.py
+++ b/app/storage.py
@@ -800,6 +800,7 @@
             )
             audit_log(
                 "vault_file_renamed",
+                connection,
                 vault_id=vault["id"],
                 vault_file_id=candidate["missing_vault_file_id"],
                 old_path=candidate["missing_path"],
```

## 7. Verification

- Regression test: `quality/test_regression.py` (BUG-009)
- Red receipt: `quality/workspace/results/BUG-009.red.log`
- Green receipt: `quality/workspace/results/BUG-009.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (gap iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
