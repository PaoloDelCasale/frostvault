# BUG-010: Configured S3 backup client failures become silent local-only success

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Gap iteration

## 1. Summary

When VAULT_S3_BUCKET is set but s3_client fails, default_object_store returns None and backups record succeeded without an off-host copy.

## 2. Spec reference

- Requirement: REQ-022
- Behavioral contract quote: "docs/metadata-backups.md: local + S3 when configured; pre-upgrade gate must not "
- CoS: Gap iteration EXPLORATION_ITER2.md / EXPLORATION_MERGED.md

## 3. The code

- Location: `app/services/metadata_backups.py:88-98`
- Documented intent: docs/metadata-backups.md: local + S3 when configured; pre-upgrade gate must not pretend off-host backup succeeded.
- Code behavior: except Exception: return None collapses configured-store failures into the unset-bucket path.

## 4. Observable consequence

When VAULT_S3_BUCKET is set but s3_client fails, default_object_store returns None and backups record succeeded without an off-host copy.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-010-fix.patch`:

```diff
--- a/app/services/metadata_backups.py
+++ b/app/services/metadata_backups.py
@@ -86,7 +86,13 @@
 
 
 def default_object_store() -> ObjectStore | None:
-    """Build the configured S3 object store, or None when bucket/credentials are unset."""
+    """Build the configured S3 object store, or None when the bucket is unset.
+
+    When a bucket *is* configured, client/credential failures raise
+    ``BackupError`` instead of returning ``None`` so callers cannot silently
+    record a successful local-only backup while believing off-host storage was
+    used.
+    """
     bucket = (settings.vault_s3_bucket or "").strip()
     if not bucket:
         return None
@@ -94,8 +100,10 @@
         from ..storage import s3_client
 
         return Boto3ObjectStore(s3_client(), bucket)
-    except Exception:
-        return None
+    except Exception as exc:  # noqa: BLE001 - normalize to BackupError
+        raise BackupError(
+            f"Configured metadata backup object store is unavailable: {exc}"
+        ) from exc
 
 
 class BackupError(RuntimeError):
```

## 7. Verification

- Regression test: `quality/test_regression.py` (BUG-010)
- Red receipt: `quality/workspace/results/BUG-010.red.log`
- Green receipt: `quality/workspace/results/BUG-010.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (gap iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
