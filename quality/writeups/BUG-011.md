# BUG-011: OIDC callback switches session for already-linked foreign identity

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Gap iteration

## 1. Summary

While user A is logged in, resolving an identity already bound to user B creates a new session for B instead of rejecting.

## 2. Spec reference

- Requirement: REQ-023
- Behavioral contract quote: "ADR-0003 self-link attaches the acting user's identity; ADR-0005 step-up refresh"
- CoS: Gap iteration EXPLORATION_ITER2.md / EXPLORATION_MERGED.md

## 3. The code

- Location: `app/main.py:1130-1146`
- Documented intent: ADR-0003 self-link attaches the acting user's identity; ADR-0005 step-up refreshes the existing session.
- Code behavior: else-branch always create_session(user_id) when session user ≠ identity user.

## 4. Observable consequence

While user A is logged in, resolving an identity already bound to user B creates a new session for B instead of rejecting.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-011-fix.patch`:

```diff
--- a/app/main.py
+++ b/app/main.py
@@ -1135,6 +1135,14 @@
             mark_reauthenticated(connection, existing["id"])
             raw_token = rotate_session(connection, existing["id"])
             csrf_token = existing["csrf_token"]
+        elif existing and existing["user"]["id"] != user_id:
+            # Do not silently switch the browser into another account when the
+            # resolved identity differs from the active Session (self-link /
+            # invite misuse of an already-bound identity).
+            raise HTTPException(
+                403,
+                "This identity belongs to a different user; sign out first",
+            )
         else:
             raw_token = create_session(
                 connection,
```

## 7. Verification

- Regression test: `quality/test_regression.py` (BUG-011)
- Red receipt: `quality/workspace/results/BUG-011.red.log`
- Green receipt: `quality/workspace/results/BUG-011.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (gap iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
