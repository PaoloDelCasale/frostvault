# BUG-012: Recover overwrites on-disk Local Copy when catalog says absent

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Unfiltered iteration

## 1. Summary

process_recover gates only on catalog local_presence then Path.replace without on-disk check.

## 2. Spec reference

- Requirement: REQ-024
- Source finding: quality/EXPLORATION_ITER3.md

## 3. The code

- Location: `app/storage.py:1899-1900 + 2024-2046`
- Divergence: process_recover gates only on catalog local_presence then Path.replace without on-disk check.

## 4. Observable consequence

See EXPLORATION_ITER3.md and BUGS.md for domain impact.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-012-fix.patch`:

```diff
--- a/app/storage.py
+++ b/app/storage.py
@@ -2043,6 +2043,10 @@
                 "Recovered plaintext digest does not match the Archive Version"
             )
         destination.parent.mkdir(parents=True, exist_ok=True)
+        if os.path.lexists(destination):
+            raise RuntimeError(
+                "A local file already exists at the recovery destination"
+            )
         temporary.replace(destination)
     finally:
         temporary.unlink(missing_ok=True)
```


- Regression test: `quality/test_regression.py` (BUG-012)
- Red receipt: `quality/workspace/results/BUG-012.red.log`
- Green receipt: `quality/workspace/results/BUG-012.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (unfiltered iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
