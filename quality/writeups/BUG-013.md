# BUG-013: Crypt recover ignores Archive Version object_key

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Unfiltered iteration

## 1. Summary

Crypt download builds rclone sources from job['path'] and ignores object_key.

## 2. Spec reference

- Requirement: REQ-025
- Source finding: quality/EXPLORATION_ITER3.md

## 3. The code

- Location: `app/storage.py:1829-1852 vs 1858-1861`
- Divergence: Crypt download builds rclone sources from job['path'] and ignores object_key.

## 4. Observable consequence

See EXPLORATION_ITER3.md and BUGS.md for domain impact.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-013-fix.patch`:

```diff
--- a/app/storage.py
+++ b/app/storage.py
@@ -1830,9 +1830,16 @@
         version_flag = f"--s3-version-id={provider_version_id}"
         if vault_encrypts_names(job):
             with vault_rclone_config(job) as runtime:
+                logical_path = object_key_to_path(
+                    object_key,
+                    job.get("s3_prefix") or "",
+                    is_crypt=True,
+                    encrypted_names=True,
+                    runtime=runtime,
+                ) or job["path"]
                 run_rclone(
                     "copyto",
-                    f"{runtime.remote_name}:{job['path']}",
+                    f"{runtime.remote_name}:{logical_path}",
                     str(temporary),
                     version_flag,
                     job_progress_callback(job),
@@ -1841,9 +1848,14 @@
                     bwlimit=job_bwlimit(job),
                 )
         else:
+            logical_path = object_key_to_path(
+                object_key,
+                job.get("s3_prefix") or "",
+                is_crypt=vault_encrypts_content(job),
+            ) or job["path"]
             run_rclone(
                 "copyto",
-                configured_rclone_destination(job, job["path"]),
+                configured_rclone_destination(job, logical_path),
                 str(temporary),
                 version_flag,
                 job_progress_callback(job),
```


- Regression test: `quality/test_regression.py` (BUG-013)
- Red receipt: `quality/workspace/results/BUG-013.red.log`
- Green receipt: `quality/workspace/results/BUG-013.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (unfiltered iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
