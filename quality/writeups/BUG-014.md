# BUG-014: User reactivation omits session_version bump

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Unfiltered iteration

## 1. Summary

update_user bumps session_version only on password change, not active transitions.

## 2. Spec reference

- Requirement: REQ-026
- Source finding: quality/EXPLORATION_ITER3.md

## 3. The code

- Location: `app/main.py:2682-2691; sessions.py:86-96`
- Divergence: update_user bumps session_version only on password change, not active transitions.

## 4. Observable consequence

See EXPLORATION_ITER3.md and BUGS.md for domain impact.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-014-fix.patch`:

```diff
--- a/app/main.py
+++ b/app/main.py
@@ -2682,6 +2682,7 @@
     if action.active is not None:
         updates.append("active=%s")
         params.append(action.active)
+        updates.append("session_version=session_version+1")
     if action.display_name is not None:
         updates.append("display_name=%s")
         params.append(action.display_name.strip())
```


- Regression test: `quality/test_regression.py` (BUG-014)
- Red receipt: `quality/workspace/results/BUG-014.red.log`
- Green receipt: `quality/workspace/results/BUG-014.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (unfiltered iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
