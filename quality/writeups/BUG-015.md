# BUG-015: Restore estimate vs worker price-book skew

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Unfiltered iteration

## 1. Summary

Estimate API uses get_active_price_book; process_recover uses hardcoded defaults.

## 2. Spec reference

- Requirement: REQ-027
- Source finding: quality/EXPLORATION_ITER3.md

## 3. The code

- Location: `app/main.py:2181-2197 vs app/storage.py:1925-1936`
- Divergence: Estimate API uses get_active_price_book; process_recover uses hardcoded defaults.

## 4. Observable consequence

See EXPLORATION_ITER3.md and BUGS.md for domain impact.

## 6. The fix

Proposed change from `quality/workspace/patches/BUG-015-fix.patch`:

```diff
--- a/app/storage.py
+++ b/app/storage.py
@@ -1922,11 +1922,16 @@
             or getattr(settings, "restore_days", 3)
             or 3
         )
+        with db() as connection:
+            from .services.cost_estimates import get_active_price_book
+
+            book = get_active_price_book(connection)
         estimate = estimate_restore(
             size_bytes=size_bytes,
             storage_class=str(storage_class),
             tier=str(tier),
             days=days,
+            pricing=book.restore_rates,
         )
         if is_high_impact_restore(
             size_bytes=size_bytes,
```


- Regression test: `quality/test_regression.py` (BUG-015)
- Red receipt: `quality/workspace/results/BUG-015.red.log`
- Green receipt: `quality/workspace/results/BUG-015.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (unfiltered iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
