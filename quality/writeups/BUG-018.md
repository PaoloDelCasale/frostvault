# BUG-018: Cancel recover clears restore_state after non-cancellable RestoreObject

> Generated by [Quality Playbook](https://github.com/andrewstellman/quality-playbook) v1.5.10 — Andrew Stellman
> Date: 2026-07-24 · Project: FrostVault · Source: Parity iteration

## 1. Summary

Cancel recover clears restore_state after non-cancellable RestoreObject

## 2. Spec reference

- Requirement: REQ-030
- Source finding: quality/EXPLORATION_ITER4.md

## 3. The code

- Location: `app/main.py cancel recover`
- Divergence: see EXPLORATION_ITER4.md / BUGS.md.

## 4. Observable consequence

See EXPLORATION_ITER4.md and BUGS.md for domain impact.

## 6. The fix

Proposed change from `quality/patches/BUG-018-fix.patch`:

```diff
diff --git a/app/main.py b/app/main.py
index 8dbc1a2..10c8905 100644
--- a/app/main.py
+++ b/app/main.py
@@ -2320,11 +2320,9 @@ def cancel_job_group(group_id: str, job_action: str, vault: dict[str, Any]):
             """,
             [message, message_key, "{}", now_iso(), *job_ids],
         )
-        if job_action == "recover":
-            ArchiveCatalog(connection).clear_restore_state_for_jobs(
-                job_ids,
-                checked_at=now_iso(),
-            )
+        # Do not clear archive_versions.restore_state on recover cancel:
+        # RestoreObject cannot be cancelled after AWS accepts it, and a later
+        # recover must be able to resume polling (REQ-030 / BUG-018).
         for row in automatic_cleanup_rows:
             audit_event_store.record_audit_event(
                 connection,
```

- Regression test: `quality/test_regression.py` (BUG-018)
- Red receipt: `quality/results/BUG-018.red.log`
- Green receipt: `quality/results/BUG-018.green.log`
- Guard: `@unittest.expectedFailure` until the fix lands in production (parity iteration verified FAIL→PASS in disposable worktrees; production tree remains unpatched).
